#!/bin/sh
set -eu
root=$(CDPATH= cd -- "${0%/*}/.." && pwd)
PATH=/opt/homebrew/bin:/opt/homebrew/opt/node@24/bin:/usr/local/bin:/usr/bin:/bin:/usr/sbin:/sbin
export PATH
exec /opt/homebrew/opt/node@24/bin/node --env-file-if-exists="$root/.env" "$root/src/alarm-cli.ts" "$@"
